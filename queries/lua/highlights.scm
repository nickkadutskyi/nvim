;; extends

((identifier) @variable.builtin
  (#eq? @variable.builtin "vim")
  (#set! @variable.builtin "priority" 130)
  )

((identifier) @variable.builtin
  (#eq? @variable.builtin "Utils")
  (#set! @variable.builtin "priority" 130)
  )
